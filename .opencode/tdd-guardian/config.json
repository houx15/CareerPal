{
  "enabled": true,
  "enforceOnTaskCompleted": false,
  "blockCommitWithoutFreshGate": false,
  "gateFreshnessMinutes": 120,
  "bypassEnv": "TDD_GUARD_BYPASS",
  "preflightCommand": "",
  "testCommand": "sh -c 'cd frontend && pnpm test --run && cd ../backend && pytest'",
  "coverageCommand": "sh -c 'cd frontend && pnpm test --run --coverage && cd ../backend && pytest --cov=app --cov-report=term --cov-report=json'",
  "coverageSummaryPath": "frontend/coverage/coverage-summary.json",
  "coverageThresholds": {
    "lines": 100,
    "functions": 100,
    "branches": 100,
    "statements": 100
  },
  "coverageMode": "no-decrease",
  "smartStaleness": true,
  "requireMutation": false,
  "mutationCommand": ""
}
